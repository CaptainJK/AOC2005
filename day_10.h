
#pragma once

#include <algorithm>
#include <ranges>
#include <string_view>
#include <vector>

struct Day10 {

  constexpr size_t id() const { return 10; }
  size_t problem1(const std::string_view &input) { return 0; }

  size_t problem2(const std::string_view &input) { return 0; }
};
